[pytest]
addopts = -ra
testpaths = tests
markers =
    smoke: critical smoke coverage
    regression: broader regression coverage
    negative: negative validation scenarios
    edge: boundary and corner-case scenarios
    e2e: end-to-end flows
